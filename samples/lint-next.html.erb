<section class="lint-next">
  <!--
    This file intentionally contains remaining lint issues.
    Supported branches should format normally and no longer lint.
  -->

  <h1>Future lint examples</h1>

  <center>Legacy heading</center>
  <div class="self-closing-card" />

  <% %>
  <%= %>

  <% if show_empty_state %>
  <% end %>

  <% if current_user %>
    <p>Welcome, <%= current_user.name %></p>
    <% else %>
    <p>Please sign in</p>
  <% end %>

  <% if show_beta_banner? %>
    <p>Try the beta dashboard</p>
  <% else %>
  <% end %>

  <% case current_user&.role %>
    <% when "admin" %>
    <% when "member" %>
    <p>Member dashboard</p>
  <% end %>

  <% while job.running? %>
  <p>Waiting for the job to finish...</p>
</section>
